name: Docker build and push to GAR

permissions:
  contents: read
  id-token: write

on:
  push:
    tags:
      - '*'
    branches:
      - 'main'
    paths:
      - '!infra/go-demo-app/**'
      - '!README.md'
  pull_request:
    branches: [ 'main' ]
    paths:
      - '!infra/go-demo-app/**'
      - '!README.md'
  workflow_dispatch:

jobs:
  generate-build-id:
    runs-on: ubuntu-latest
    outputs:
      BUILD_ID: ${{ steps.prep.outputs.BUILD_ID }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Generate build ID
        id: prep
        run: |
          branch=${GITHUB_REF##*/}
          sha=${GITHUB_SHA::8}
          ts=$(date +%s)
          echo "BUILD_ID=v0.1.0-${branch}-${sha}-${ts}" >> $GITHUB_OUTPUT

  build-and-push-docker-image-to-gar:
    needs: generate-build-id
    uses: andreistefanciprian/gh-reusable-workflows/.github/workflows/build_and_push_docker_image_to_gar.yaml@main
    with:
      app_name: go-demo-app
      build_id: "v0.1.0-${{ github.run_number }}"
    secrets: inherit
